import 'login_screen.dart';

/// Écran d'inscription.
/// Alias de [PremiumSignupScreen] pour simplifier le routage.
class SignupScreen extends PremiumSignupScreen {
  const SignupScreen({super.key});
}
